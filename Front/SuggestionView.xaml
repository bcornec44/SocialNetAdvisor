<UserControl x:Class="SocialNetAdvisor.SuggestionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <TextBox Text="{Binding SelectedTextHtml, Mode=TwoWay}" Height="100" IsReadOnly="True"/>
        <TextBox Text="{Binding SuggestionHtml, Mode=TwoWay}" Height="200"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Command="{Binding CancelCommand}" Content="Cancel" Background="Orange"/>
            <Button Command="{Binding SettingsCommand}" Content="Settings"/>
            <Button Command="{Binding GenerateCommand}" Content="Generate"/>
            <Button Command="{Binding CopyCommand}" Content="Copy" Background="Green"/>
        </StackPanel>
    </StackPanel>
</UserControl>
